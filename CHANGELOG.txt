ROCCAT Manager Full — Changelog
================================

@6 — 2026-09-19 (stuck-modifier fix, busy lock, active-profile memory)
  - Stuck keyboard modifier fix: a mouse button bound to Ctrl/Alt/Shift/Win could leave
    that modifier "held" in Windows after a profile switch (keyboard goes dead). New
    kone_xp_air/winput.py release_modifiers() sends synthetic key-ups (ctypes SendInput,
    no-op off Windows); run_mouse_job fires it after every mouse op, and a "Release stuck
    keys" footer button + POST /api/release-modifiers give a one-click manual recovery
    (the endpoint deliberately does NOT take MOUSE_LOCK so it works mid-push).
  - Packaging fix: kone_xp_air.winput added to roccat_manager.spec hiddenimports — server.py
    imports it at load, so without it the windowed exe closed on launch (ModuleNotFoundError).
  - Busy lock: while a push/switch rewrites onboard memory (~15 s) the UI shows a "Writing
    to the mouse..." overlay and refuses any new mouse operation, so two writes can't overlap
    (an overlapping write can corrupt a profile or strand a modifier). Wraps push-slot,
    push-to-mouse, push-all, and switch.
  - Active-profile memory: the onboard slot the mouse was last set to is persisted
    (slots.json "active_slot", written in run_mouse_job) and returned by GET /api/slots/{boot};
    a fresh launch now highlights the profile the mouse is on instead of nothing.
  - Keybind list gained the bare modifiers (Ctrl/Shift/Alt/Win + right variants) for AutoCAD.
  - Tests: 94 pass (active-slot persistence + read-does-not-clobber added).

@5 — 2026-09-19 (hardware confirmed + desktop app)
  - DIRECT WRITES CONFIRMED on the mouse (A/B variant B): our own handle changed
    a button with Swarm II out of the write path. Default transport is now Direct.
  - DirectHidTransport backend "auto": bundled hidapi first, Swarm's KONE_XP_AIR.dll
    fallback; send retry for transient rc=-1.
  - Native desktop app: desktop.py (pywebview window, no browser/localhost),
    roccat_manager.spec + build.bat -> single dist\ROCCAT Manager.exe. server.py is
    bundle-aware (templates/static via sys._MEIPASS) and stores profiles under
    %APPDATA%\ROCCAT Manager.
  - Hardware-path review applied (frida 17 PID lookup, abort-before-commit,
    executor census guard, ab_test transcript + distinct-letter variants).

@4 — 2026-09-19 (cloud session, no hardware)
  - kone_xp_air/: hardware-independent protocol package (packets, 125-byte button
    block = 3 header + 30 x 4-byte entries + sum16, 75-byte profile block, action
    table, op-list sequences reproducing the captured traffic, recording / direct /
    Frida transports, .dat reader). 88 tests; Swarm's own .dat exports round-trip
    byte for byte.
  - Diagnosis: the April "must use Swarm's handle" conclusion compared different
    bytes (07 7d header, invented 0x49 commit, no 0x45/0x4e activation, Swarm
    killed) — the working bytes were never sent over a plain handle. Bugs fixed:
    right-button offset 8 -> 9, push targeting the clicked slot, unknown actions
    silently disabled, dead /api/write-buttons import, heap-read overwriting
    stored keybinds.
  - server.py + index.html on the package: per-slot push (/api/push-slot), push
    all, transport switch (profiles/mouse_config.json), /api/mouse/status,
    /api/import-dat; heap-scan live read removed.
  - tools/: diag_swarm_handle.py, ab_test_buttons.py (A-F variants),
    dump_mouse_pages.py; RUN_ON_PC.md.
  - HANDOFF.md rewritten; CLAUDE.md / README updated; INSTALL_AND_RUN.bat installs
    flask frida hidapi.

@3 — 2026-04-10..12 (reconstructed from git; was never recorded here)
  - Direct DPI write (0x46 pages + commit) and profile switch (0x45/0x4e) over
    KONE_XP_AIR.dll's hidapi on the dongle usage page 0xFF03 (roccat_write.py).
  - Button writes (0x47) over the same handle did not change behaviour; byte-exact
    replay through Swarm II's own handle via Frida did (inject_full.js).
  - frida_inject.py: switch / push / heap-scan read_profiles wired into server.py.
  - Swarm II INI decoding (swarm_ini.py), named-pipe IPC research, pyautogui
    automation attempts.

@2 — 2026-03-23
  - HID protocol reverse-engineering session (Boot 1)
  - Built hid_spy.py: real-time HID report monitor
  - Discovered dongle (PID 0x5017) is the command gateway to mouse
  - Mapped mouse report layout: 0x01=DPI+LED, 0x02=buttons, 0x03-05=LEDs, 0x06/08=DPI stages
  - Confirmed mouse IF=1 (0xFF00) as write channel
  - Tested every write method (hidapi, pyusb, ctypes, frida) — none change actual DPI
  - Researched Kone Pro protocol (same generation): SET_REPORT control transfers, 69-byte settings
  - Installed Wireshark 4.6.4, frida for future capture work
  - Conclusion: need USB packet capture from Boot 2 to decode write protocol
  - Added capture/probe tools: hid_spy, hid_dongle_write, hid_brute_write, ctypes_hid_write, etc.

@1 — 2026-03-23
  - Initial project registration in Projects Folder workflow
  - Created CLAUDE.md, HANDOFF.md, CHANGELOG.txt
  - Core app already complete from prior build session (Boot 2/claude.ai):
    - Flask backend + full REST API (port 5555)
    - Dark-themed SPA frontend (index.html)
    - Boot 1 profiles fully configured (5 profiles)
    - Binary .dat format reverse-engineered (SWARM_II_DAT_FORMAT.py, profile_mgr_format.py)
    - .dat export/import to SWARM II onboard file
    - Launcher scripts (INSTALL_AND_RUN.bat, Launch.bat)
    - pywinauto automation stubs (needs calibration)
    - HID protocol experiments (root hid_*.py, not integrated)
